# Copilot Instructions

## Project Guidelines
- When modifying studio.testing.vcxproj include paths, use project-aligned paths like $(ProjectDir)..\..\library\mysql.canvas\src instead of hard-coded or inconsistent relative paths.
- When troubleshooting missing includes, prefer fixing the actual include/file path issue in the workspace rather than proposing hypothetical snippets.